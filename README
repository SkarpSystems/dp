Install jasmine globally
> sudo npm install -g jasmine

Install required dependencies:
> npm update

Install extra dependencies for development:
> npm --dev update

Run tests:
> npm test

Start local web browser: nodejs index.js

Connect browser to http://localhost:36080
